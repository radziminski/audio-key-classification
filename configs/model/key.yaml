_target_: src.models.key_classifier.KeyClassifier
learning_rate: 0.005

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.005
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.4
  patience: 2

model:
  _target_: src.models.components.allconv.AllConv
  num_feature_maps: 16
  dropout_rate: 0.1
  input_channels: 1
  num_classes: 24
